# WriterStudio 运行依赖
PySide6>=6.6
qtawesome>=1.3          # 图标（FontAwesome 6，MIT）
pyqtdarktheme-fork>=2.3  # 扁平深浅色主题（MIT，PyQtDarkTheme 维护分支）
fontTools>=4.40
pyserial>=3.5
numpy>=1.24
matplotlib>=3.7        # LaTeX 公式（mathtext）渲染
markdown-it-py>=3.0    # Markdown 解析
svgelements>=1.9       # SVG 矢量图导入
shapely>=2.0           # 几何运算（矢量处理备用）

# 完整 LaTeX 文档（可选）需系统提供：
#   xelatex / pdflatex（texlive，含 texlive-latex 提供 latex.ltx）
#   pdftocairo（poppler）或 dvisvgm
