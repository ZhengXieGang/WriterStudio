"""AI 智能排版服务（MCP）。

让 AI agent（Claude Desktop 等任何 MCP 客户端）连接本软件，直接完成整页
排版并产出可发送打印的内容。架构参考成熟的 blender-mcp 双进程模式：

    ┌─────────────┐  stdio(MCP)  ┌──────────────┐   TCP(本机回环)   ┌────────────┐
    │ MCP 客户端   │ ───────────▶ │ ai.bridge    │ ────────────────▶ │ GUI 内嵌    │
    │ (Claude 等) │              │ (协议翻译桥)  │   127.0.0.1:端口  │ ai.server  │
    └─────────────┘              └──────────────┘                   └────────────┘

* ``ai.server``  —— GUI 进程内的 QTcpServer，主线程事件驱动（无锁、无线程
  安全问题），把请求分派给 :class:`ai.tools.AiTools`；
* ``ai.tools``   —— 工具层：坐标语义、参数校验、内容生成与布局检查，全部
  走既有控制器命令（可撤销）；
* ``ai.bridge``  —— 独立进程的 stdio MCP 服务器（官方 ``mcp`` SDK），任何
  MCP 客户端都能即插即用；GUI 无需依赖 ``mcp`` 包。

坐标约定（对 AI 暴露的语义，与内部模型相反）：
    原点在页面**左上角**，x 向右、y 向下，单位 mm。所有涉及位置/包围盒的
    工具入参与返回值都用这套语义，由本包负责与内部「左下原点、y 向上」
    互转——AI 不需要知道内部约定。
"""
